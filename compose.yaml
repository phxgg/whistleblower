services:
  bot:
    build: .
    restart: unless-stopped
    depends_on:
      - redis
    volumes:
      - ./config.json:/app/config.json:ro
      - ./logs:/app/logs

  redis:
    image: redis:8-alpine
    restart: unless-stopped
    volumes:
      - redis-data:/data

volumes:
  redis-data:
